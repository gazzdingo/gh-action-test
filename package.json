{
  "name": "growthbook-test-app",
  "version": "1.0.0",
  "description": "Test application for GrowthBook feature flags and coderefs action",
  "private": true,
  "dependencies": {
    "@growthbook/growthbook-react": "^0.17.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "count-flags": "node scripts/count-flags.js",
    "count-flags-json": "node scripts/count-flags.js --json"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
